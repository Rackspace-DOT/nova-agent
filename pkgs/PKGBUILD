pkgname=nova-agent
pkgver=0.1
pkgrel=1
pkgdesc='Setup servers from xenstore'
arch=('any')
url='https://github.com/gtmanfred/novaagent'
license=('custom')
depends=('python2-crypto')
makedepends=('python2-setuptools')
source=("$pkgname::git+git://github.com/gtmanfred/novaagent.git")
md5sums=('SKIP')

package(){
    cd $srcdir/$pkgname
    python2 setup.py install --root=$pkgdir
    install -Dm644 etc/nova-agent.service $pkgdir/usr/lib/systemd/system/nova-agent.service
}
