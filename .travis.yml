
language: python
python:
  - "3.5"
install:
  - pip install -r requirements.txt
  - pip install -r opt_requirements.txt
  - pip install coverage flake8 funcsigs
before_script:
  - flake8 .
script: nosetests -v --cover-erase --with-coverage --cover-package=novaagent
